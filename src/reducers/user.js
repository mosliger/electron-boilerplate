// import * as ActionTypes from '../actions/actionType';

const innitialState = {
  isLogin: true
};

export default (state = innitialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
